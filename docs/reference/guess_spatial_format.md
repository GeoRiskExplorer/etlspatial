# Guess spatial file format

Guess spatial file format

## Usage

``` r
guess_spatial_format(dsn)
```

## Arguments

- dsn:

  Path to a spatial data source.

## Value

One of `"gdb"`, `"shapefile"`, or `"gpkg"`.
